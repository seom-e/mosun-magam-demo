/* ─────────────────────────────────────────────────────────────────────────
   담당자 명부 — 누가 어느 서비스를 맡는가

   **사람이 늘거나 담당이 바뀌면 이 파일만 고치면 된다.**
   시작 화면 · 설정 화면 · 스케줄확인이 모두 이 파일을 읽는다.

   메일 주소는 여기 적지 않는다. 그건 각자 자기 컴퓨터에서 [설정] 에 넣는다.
   여기에는 **이름과 담당 서비스만** 둔다.

   원본은 `모선마감\체크리스트\내서비스.txt` — 담당이 바뀌면 그 파일과 여기를 같이 고친다.
   ───────────────────────────────────────────────────────────────────────── */
window.MOSUN_OWNERS = {
  '홍길동': ['SIS2','FEM','PCI','PCI2','KI1','NSB','KXS1','BNJ','KBX','NKX','CKX3','ADHS'],
  '김철수': ['KJS1','KJS2','KJS7','KJS10'],
  '이영희': ['CKX1','NTX','KHS1','BTS','KQS','BSS1','VTS','HPS1']

  // 사람이 늘면 이 아래에 한 줄 더한다. 예)
  // ,'김과장': ['ABC1','ABC2']
};
